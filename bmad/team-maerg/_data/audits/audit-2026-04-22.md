---
date: 2026-04-22
run_status: completed
reconciliation: clean
findings_count: 7
drift_count: 0
agents_active: 1
---

# Reconciliation

All 8 coherence checks pass for the single active agent `team-maerg/chuck`. No drift between registry, source YAML, compiled MD, Claude command, agent-manifest.csv, and manifest.yaml. Name and status coherent.

One spec-clarity issue surfaced during reconciliation and carried forward to the Summary (not drift):

- Registry `id` uses `<module>/<short-name>` form (`team-maerg/chuck`). Agent YAML `metadata.id` uses compiled-path form (`bmad/team-maerg/agents/chuck.md`). The audit spec's `{id}` substitution in the four file-existence checks assumes a single id convention. Both resolve to the same agent, so reconciliation passes under pragmatic interpretation, but the spec itself doesn't crisply document the convention bridge. Per team principle 3 (spec ambiguity is a bug in the spec), logged as a recommended amendment to `audit-ecosystem/instructions.md` step 2 and/or a registry-schema normalization.

# Agent-Vintage Signals

| id               | src_mtime           | compiled_md_mtime   | claude_cmd_mtime    | stale_compile | git_last_touched | uncommitted | compile_missing |
| ---------------- | ------------------- | ------------------- | ------------------- | ------------- | ---------------- | ----------- | --------------- |
| team-maerg/chuck | 2026-04-22T03:26:56 | 2026-04-22T03:30:01 | 2026-04-22T03:30:02 | false         | null             | true        | false           |

Notes:

- `stale_compile: false` — source predates both compiled artifacts by ~3 minutes; compile is current relative to source.
- `git_last_touched: null, uncommitted: true` — `src/modules/team-maerg/agents/chuck.agent.yaml` is untracked (git status confirms `?? src/modules/team-maerg/`). The agent exists in the working tree but not in version control. This bypasses the Gap 3 edit-stale comparison (no git timestamp to compare against `last_reviewed`).

# Scope Overlap — Jaccard (4a)

Skipped — N≤1, no pairs to compute.

# Scope Overlap — LLM Judgment (4b)

Skipped — N≤1, no pairs to compute.

# Scope Overlap — Joined View

Skipped — N≤1, no pairs to reconcile.

# Derived Surface Report

## team-maerg/chuck

**reads:**

- `bmad/team-maerg/config.yaml` (critical_actions)
- `bmad/team-maerg/_data/team-principles.md` (critical_actions)
- `bmad/team-maerg/_data/ecosystem-registry.yaml` (critical_actions + `*registry` action)
- `bmad/team-maerg/_data/lens-framework.md` (persona.identity references — _missing on disk; see Orphan Scan_)
- `src/modules/team-maerg/agents/*.agent.yaml` (via audit-ecosystem workflow step 1)
- `bmad/team-maerg/agents/*.md` (via audit-ecosystem workflow step 1)
- `.claude/commands/bmad/team-maerg/agents/*.md` (via audit-ecosystem workflow step 1)
- `bmad/_cfg/agent-manifest.csv` (via audit-ecosystem workflow step 1)
- `bmad/_cfg/manifest.yaml` (via audit-ecosystem workflow step 1)
- `bmad/team-maerg/workflows/**` (glob, via audit-ecosystem workflow step 1)
- `bmad/team-maerg/workflows/audit-ecosystem/instructions.md` (workflow instructions)
- `bmad/team-maerg/workflows/audit-ecosystem/checklist.md` (workflow validation)

**writes:**

- `bmad/team-maerg/_data/audits/audit-{{date}}.md` (audit-ecosystem workflow default_output_file)
- `bmad/team-maerg/_data/ecosystem-registry.yaml` (conditional, via audit-ecosystem step 9 — last_reviewed updates; gated behind per-agent user confirmation)

**invokes:**

- `audit-ecosystem` workflow — valid, resolves to `bmad/team-maerg/workflows/audit-ecosystem/workflow.yaml`
- `propose-agent` — **todo** (placeholder, dangling)
- `evolve-agent` — **todo** (placeholder, dangling)
- `retire-agent` — **todo** (placeholder, dangling)
- `architecture-decision` — **todo** (placeholder, dangling)
- `*registry` — inline action (reads ecosystem-registry.yaml; no workflow delegation)

**tools:**

- `Bash` — declared in audit-ecosystem `required_tools`; scoped uses: `mkdir`, `stat`, `git log --follow --format=%aI -1 -- <path>`, `date +%H%M%S`. Read-only or scoped-create only. No network, no arbitrary execution surface.
- `Read`, `Edit`, `Write` — implicit BMAD baseline. Writes scoped to audit output and (conditionally, behind user gate) ecosystem-registry.

**Permission-surface assessment vs capability_tier=strategic:**
Derived surface is consistent with a meta-layer architect role. Writes are limited to:
(a) audit records the agent produces itself, and
(b) the ecosystem registry behind an explicit per-agent user gate.
No surprising surface. No expansion of blast radius beyond registry + audit-folder + git-read.

# Registry-Quality Gaps

| agent            | Gap 1 (eval)     | Gap 2 (time-stale >90d) | Gap 3 (edit-stale)                                |
| ---------------- | ---------------- | ----------------------- | ------------------------------------------------- |
| team-maerg/chuck | ✓ rubric present | ✓ 0 days since review   | N/A — uncommitted, no git_last_touched to compare |

No flags. Chuck's `success_rubric` is well-formed and three-pronged: (a) proposed-agent ship/reject traceability, (b) zero registry-filesystem drift across audits, (c) no permission expansion without blast-radius note. `last_reviewed: 2026-04-22` equals today (creation day).

# Orphan Scan

## Orphans (files present, zero references)

None. All files under `bmad/team-maerg/_data/` are referenced:

- `ecosystem-registry.yaml` — referenced by `config.yaml:20`, Chuck's critical_actions[2], Chuck's `*registry` action.
- `team-principles.md` — referenced by `config.yaml:24`, Chuck's critical_actions[1].
- `audits/` — excluded by glob (audit outputs are valid by design).

## Dangling References (references that point nowhere)

1. **`bmad/team-maerg/_data/lens-framework.md` — MISSING** (top-priority). Referenced by Chuck's persona.identity as "Full framework reference lives in {project-root}/bmad/team-maerg/\_data/lens-framework.md — loaded JIT when a command requires deep reference." The file does not exist on disk (verified via `ls bmad/team-maerg/_data/` — contents: `audits/`, `ecosystem-registry.yaml`, `team-principles.md`). Chuck's identity names the 17-lens framework tiered by blast radius; without the reference file, JIT-load per Chuck's persona fails. This is a principle-1 (fact-at-emission) breach in Chuck's own identity.

2. `propose-agent` workflow path = `"todo"` — placeholder, no workflow.yaml exists.
3. `evolve-agent` workflow path = `"todo"` — placeholder.
4. `retire-agent` workflow path = `"todo"` — placeholder.
5. `architecture-decision` workflow path = `"todo"` — placeholder.

Placeholders 2–5 are by design (menu lists capabilities not-yet-implemented). They are referenced in Chuck's description as if buildable today; the `workflow="todo"` sentinel correctly marks them as unbuilt. Not a bug, but tracked as **known dangling refs** until workflows are authored.

# Summary & Prioritized Actions

Reconciliation is clean across all 8 checks for the single active agent. Total findings: 7.

## By severity (drift > permission > overlap > eval > staleness > orphans)

### Permission surface (2 findings)

1. **Registry id-schema ambiguity** (P1 — spec clarity). Registry uses `team-maerg/chuck`, YAML metadata.id uses `bmad/team-maerg/agents/chuck.md`. Reconciliation passes under pragmatic interpretation, but the audit workflow spec itself doesn't document the id-bridge. Action: amend `audit-ecosystem/instructions.md` step 2 to make the id-resolution algorithm explicit (pseudocode per team principle 3), OR normalize the registry schema to one form. Recommend the former — cheaper and preserves the short-form registry readability.
2. **Chuck source is uncommitted** (P1 — reproducibility). `src/modules/team-maerg/agents/chuck.agent.yaml` is not under version control (confirmed via `git status`: `?? src/modules/team-maerg/`). The audit cannot compute Gap 3 edit-staleness for Chuck, and the agent definition is not yet reproducible from git. Action: commit the team-maerg source bundle before the next audit cycle.

### Overlap (0 findings)

Skipped — N≤1.

### Eval (0 findings)

All active agents have well-formed `success_rubric`.

### Staleness (0 findings)

`last_reviewed` is current (0 days). Edit-based staleness cannot fire with uncommitted source — flag rolls forward to next audit once source is committed.

### Orphans / dangling refs (5 findings — 1 high, 4 known)

6. **`lens-framework.md` MISSING — HIGH.** Referenced in Chuck's persona.identity; not present on disk. Violates principle 1 in Chuck's own persona (names a specific mechanism and location without verifying existence). Action: either (a) author `bmad/team-maerg/_data/lens-framework.md` with the full 17-lens reference, or (b) inline the framework into persona.identity and remove the external reference. Prefer (a) — JIT-loading a fat reference is exactly the intent, and persona bloat is the failure mode of (b).
7. **4 `workflow="todo"` placeholders** (propose-agent, evolve-agent, retire-agent, architecture-decision). Known and expected until workflows are authored. Action: build in order of operational need; the next forced build will be whichever menu item gets invoked first.

## Next action pointer

Highest-severity actionable finding: **`lens-framework.md` is missing.** Chuck claims JIT-load of a 17-lens reference framework that does not exist on disk. Next step is either author the file or amend persona.identity to match reality. Recommend authoring — Chuck's identity structure already lists the 17 lenses by name and tier.

Full audit record written to `bmad/team-maerg/_data/audits/audit-2026-04-22.md`.

# Registry Updates

No edit-stale agents (Gap 3 is N/A for Chuck because `git_last_touched` is null — uncommitted source). No prompts issued, no updates performed.
